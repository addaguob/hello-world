#include <stdio.h>
#include <cs50.h>

int main(int argc, string argv[])
{
    printf("Hello, %s!\n", argv[1]);
    printf("argc is %i!\n", argc);
    printf("argv[0] is %s and argv[1] is %s!\n", argv[0], argv[1]);
}